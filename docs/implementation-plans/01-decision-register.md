# Decision Register And Implementation Gates

These decisions are absent or contradictory in the specifications. Resolve and record each decision before implementing the affected slice. Do not silently select placeholder values in production code.

## Resolved Interpretations

| ID | Decision | Rationale |
| --- | --- | --- |
| R1 | `Esc` during transcription enters `Transcribing(Cancelling)` until the matching worker outcome arrives. | This is required by functional spec sections 5 and 16 and architecture sections 6, 16, 17, and ADR-11. It supersedes the contradictory immediate-Ready diagram in architecture section 39. |
| R2 | Every transcription gets a monotonically increasing `TranscriptionId`; every terminal worker event includes it. | Architecture section 7 says IDs are mandatory, and they prevent stale events and cancellation races. |
| R3 | A completion received after cancellation was requested is discarded, even if inference finished first. | Cancellation has precedence once the controller processes `Esc`; no text is printed or copied during the cancelling phase. |
| R4 | Clipboard outcome is separate from transcription outcome. | A successful transcription remains printed and successful if copying fails. |

## Blocking Decisions

| ID | Required decision | Affected slices | Completion evidence |
| --- | --- | --- | --- |
| D1 | Pinned model source: compatible multilingual Whisper `base` artifact URL, version/revision, filename, SHA-256, cache directory, cache validation frequency, partial-download cleanup, and atomic replacement strategy. | 1+ | ✅ Resolved during slice 1 — see Slice 1 Gate Resolutions below. |
| D2 | Exact `whisper-rs` version and verified cooperative-cancellation API. Define its callback/abort wiring, expected cancellation latency, and fallback if native interruption is unavailable. | 1, 4, 6, 7 | ✅ Resolved during slice 1 — see Slice 1 Gate Resolutions below. |
| D3 | Linux support baseline and native dependency/packaging policy for CPAL, arboard, and whisper.cpp. | 1, 7 | ✅ Resolved during slice 1 — see Slice 1 Gate Resolutions below. |

## Slice 1 Gate Resolutions (D1–D3)

Resolved 2026-08-10 while implementing slice 1. Blocking rows D1–D3 are satisfied; D4–D6 remain blocking for later slices.

| ID | Resolution | Evidence |
| --- | --- | --- |
| D1 | Pinned artifact: `ggml-base.bin` (multilingual Whisper `base`, float32) from `https://huggingface.co/ggerganov/whisper.cpp/resolve/main/ggml-base.bin`. SHA-256 `60ed5bc3dd14eea856493d334349b405782ddcaf0028d4b5df4088345fba2efe`; size 147,951,465 bytes. Cache directory: `$XDG_DATA_HOME/transclip/models/` (default `~/.local/share/transclip/models/`), resolved via `dirs::data_dir()`. Validation frequency: SHA-256 re-verified at every process start; a corrupt or stale cached artifact is deleted and re-downloaded. Partial downloads stream into `<filename>.part` inside the cache directory; any failure (HTTP, write, checksum) removes the temp file; stale `.part` files are removed before a new download. Atomic replacement: verify the temp file, then `fs::rename` (atomic on the same filesystem). Cache dirs/files use standard XDG data-dir permissions (0755 dirs, 0644 files under the default umask); the cache is single-user. | Constants and provenance in `src/transcriber.rs`; checksum, download, and corruption fixture tests in `transcriber::tests`; full-download verification against the HF `x-linked-etag` on 2026-08-10. |
| D2 | `whisper-rs = 0.16.0` (whisper-rs-sys 0.15.0, vendored whisper.cpp 1.8.3; CPU-only default features; whisper.cpp compiled from source via cmake at build time). Load API: `WhisperContext::new_with_params(path, WhisperContextParameters::default())`; the context is `Send + Sync`, so the slice-5 long-lived worker can own it (handoff: `main` keeps the context alive for the session in slice 1). Cooperative cancellation: `FullParams::set_abort_callback_safe(FnMut() -> bool)` is polled by whisper.cpp during inference; exact wiring, cancellation latency, and fallback if native interruption is unavailable are validated in slices 4/6/7. | Vendor source inspected (`whisper-rs` 0.16.0, `whisper-rs-sys` 0.15.0); version pinned in `Cargo.toml`. |
| D3 | Baseline: Linux x86_64 desktop. Build prerequisites: cargo/rustc, cmake ≥ 3.5, a C/C++ compiler, and libclang (bindgen); runtime: glibc, libstdc++, libm, no OpenSSL (HTTPS via rustls). Validated on Ubuntu 24.04 (gcc 13.3, cmake 3.28, clang 18). Unsupported runtime: a non-Linux OS produces a clear startup error and exits non-zero before any interactive state is entered. CPAL/arboard native-dependency policies are deferred to slices 3/5; packaging is deferred to slice 8. | `ModelError::UnsupportedRuntime` path in `src/main.rs`; build prerequisites recorded here. |
| D4 | Audio contract: target Whisper sample rate, supported CPAL sample formats/channel layouts, resampler, silence/empty-recording behavior, and stop/flush failure behavior. | 3+ | Unit fixtures establish conversion/downmix/resampling behavior. |
| D5 | Worker lifecycle: bounded job protocol, startup handshake, submission failure, panic policy, shutdown signal/acknowledgement, join timeout, and repeated Ctrl+C behavior. | 4, 6, 7 | ✅ Resolved during slice 4 — see Slice 4 Gate Resolutions below. |
| D6 | Terminal operational policy: append/redraw approach, resize handling, runtime I/O failure, raw-mode Ctrl+C behavior, and shutdown wait policy. | 1, 2, 7 | ✅ Resolved during slice 2 — see Slice 2 Gate Resolutions below. |

## Slice 2 Gate Resolutions (D6)

Resolved 2026-08-10 while implementing slice 2. Blocking row D6 is satisfied; D4–D5 remain blocking for later slices.

| ID | Resolution | Evidence |
| --- | --- | --- |
| D6 | Terminal operational policy: **append-only line rendering** — every view change appends a status block below the persistent history (previous statuses, results, errors); no full-screen clear/redraw and no absolute cursor positioning (architecture section 23). **Resize handling**: resize events are non-key events, ignored by the input layer; append-only rendering is layout-independent. **Runtime I/O failure**: any read/write error propagates as `TerminalError` to `main`, which reports and exits non-zero; the RAII `TerminalGuard` restores raw mode and cursor visibility on the way out (architecture section 24). **Raw-mode Ctrl+C**: with raw mode enabled Ctrl+C arrives as a key event, not SIGINT; it remains the exit key (slice-1 behavior, architecture section 29) and initiates the normal shutdown path — slice 2 has no worker, so shutdown discards any active recording (recorder dropped) and restores the terminal; worker-stop waiting is added with the worker (slices 4/6/7, D5). **Bounded input polling**: terminal input is polled with a 100 ms bounded timeout so worker events are drained promptly and inference never blocks the loop (functional spec 10, architecture section 8). **Key mapping** (functional spec 8, architecture section 20): commands are produced only from `KeyEventKind::Press` (Repeat/Release ignored — prevents hold-to-toggle); `Ctrl+R` requires exactly the CONTROL modifier on `Char('r')`; `Esc` maps from `KeyCode::Esc` regardless of modifiers; all other keys ignored. No OS-level key hooks are installed. | Verified against the crossterm 0.29.0 source in the local registry: `KeyEvent { code, modifiers, kind, state }`, `KeyEvent::new` defaults `kind` to `Press`, `KeyEventKind::{Press,Repeat,Release}`, `event::poll(Duration)` / `event::read()`, `Event::{Key,Mouse,Resize,FocusGained,FocusLost,Paste}`. Poll timeout and key-kind/modifier selection are implemented in `src/terminal.rs` (`POLL_TIMEOUT`, `map_key`, `read_event`); append-only rendering in `terminal::TerminalRenderer`; the Ctrl+C shutdown path in `app::run` plus `main.rs`. Manual pty checks on 2026-08-10 cover status rendering, clean exit, and terminal restoration. |

### Slice 2 notes

- Interpretations R1–R4 are applied: `Esc` during transcription enters `Transcribing(Cancelling)` until a matching worker outcome arrives (R1); every transcription carries a monotonic `TranscriptionId` and every terminal worker event includes it (R2); a completion received while cancelling is discarded (R3); clipboard outcome is separate and out of slice-2 scope (R4).
- The plan's automated-test line mentions a fake clipboard; slice 2 introduces no clipboard boundary because clipboard behavior is slice 5 (index row 5, architecture section 18) and adding one would be speculative. App tests substitute the recorder with a fake, the worker sender with a real `mpsc` channel, and the output sink with assertions on `AppOutcome` (view change + output lines) — no real terminal, microphone, or Whisper dependency.
- `RecordedAudio` lives in `src/recorder.rs` (it is the recorder boundary's output, architecture section 10) so that `recorder` does not depend on the controller; the shared-contract shape is preserved. The `terminal` module imports `app::UserCommand` for key mapping because architecture section 20 assigns the key→command mapping to the terminal input layer.

## Slice 3 Gate Resolutions (D4)

Resolved 2026-08-10 while implementing slice 3. Blocking row D4 is satisfied; D5 remains blocking for slice 4.

| ID | Resolution | Evidence |
| --- | --- | --- |
| D4 | **Target sample rate**: 16,000 Hz — `WHISPER_SAMPLE_RATE` from whisper-rs-sys 0.15.0 / vendored whisper.cpp 1.8.3 (`bindings.rs`). **CPAL**: pinned `cpal = 0.18.1` (latest as of 2026-08-10, MSRV 1.85); Linux ALSA is the default host with no feature flags. Open the default input device with its default config; if that config's sample format is unsupported, fall back to the best `supported_input_configs` entry by `cmp_default_heuristics` (prefer 48 kHz, then 44.1 kHz, then max rate). Stream built via `build_input_stream_raw` with a 2 s init timeout so a hung device fails `start` instead of blocking the loop. **Supported sample formats**: I8, I16, I24, I32, U8, U16, U24, U32, F32, F64 — each addressable via `Data::as_slice::<T>()` (I24/U24 are cpal's re-exported dasp_sample types); conversion to `f32` via `dasp_sample::Sample::to_sample::<f32>()` (dasp_sample 0.11 is already a cpal dependency — no new crates). DSD (non-PCM) and I64/U64 are rejected at `start` with a clear error, leaving the controller Ready (functional spec 15.1). **Channel layout**: any channel count ≥ 1; captured interleaved as `f32`, downmixed to mono at stop by per-frame averaging. **Resampler**: linear interpolation implemented as pure functions in `src/audio.rs` — no new dependency (architecture section 52: keep dependency additions conservative); deterministic, fixture-tested, adequate for speech at 16 kHz; candidate for upgrade if quality issues appear. **Empty/silent/too-short behavior**: `stop` rejects a completely empty capture (zero samples — e.g. a near-instant toggle) with `RecorderError("no audio was captured")`, so no job is submitted; any non-empty audio passes through — whisper.cpp handles n_samples == 0 by returning success with empty text (verified: `whisper_full_with_state` guards mel conversion with `if (n_samples > 0)`), so silent audio yields empty transcription text, not an error. No arbitrary duration threshold. **Stop/flush failure**: `stop` is infallible apart from the empty rule — drop the stream first (CPAL `Drop` joins the ALSA callback thread, so no further writes), then lock the shared buffer and `mem::take`; `cancel` drops the stream and clears the buffer; both are idempotent. **Stream errors**: the CPAL error callback only sends `AppEvent::RecordingFailed(message)` on the event channel and never touches the stream (dropping the stream inside its own callback thread would self-join); the controller processes the event on the main loop and calls `cancel`, which releases the stream there (functional spec 15.2). | Constants and provenance in `src/recorder.rs`/`src/audio.rs`; conversion/downmix/resampling fixtures in `audio::tests`; buffer accumulation/transfer, cancel-clear, and repeated-cleanup tests in `recorder::tests`; app-level start/stream-failure-to-Ready tests in `app::tests`. Verified against the cpal 0.18.1 and dasp_sample 0.11 sources downloaded from static.crates.io on 2026-08-10 (`traits.rs`, `lib.rs`, `sample_format.rs`, `host/alsa/mod.rs`) and whisper-rs-sys 0.15.0 vendored whisper.cpp 1.8.3. |

## Slice 4 Gate Resolutions (D5)

Resolved 2026-08-10 while implementing slice 4. Blocking row D5 is satisfied; the worker protocol tests in `transcriber::tests` cover each terminal outcome with a fake transcriber.

| ID | Resolution | Evidence |
| --- | --- | --- |
| D5 | **Worker lifecycle** (worker lives in `src/transcriber.rs`; plan file `05-worker-transcription-flow.md`):  - **Bounded job protocol**: job channel is `mpsc::sync_channel(1)` — at most one queued job, no backlog. The state machine guarantees the controller never submits while a job is outstanding (it only submits from `Recording` and stays `Transcribing` until the worker's outcome), so `send` never blocks; the bound is the enforcement arm if that invariant is ever violated. A job carries `TranscriptionId`, normalized `RecordedAudio`, and the shared `Arc<AtomicBool>` cancellation flag (architecture section 15, decision R2).
  - **Startup handshake**: `transcriber::spawn_worker` runs a factory on the worker thread (`WhisperTranscriber::new` creates the `WhisperState` from the already-loaded `WhisperContext`) and reports `Result<(), String>` over a one-shot channel before the app becomes Ready. `spawn_worker` blocks on the handshake; `Err` → `ModelError::WorkerStartup` → `main` exits non-zero without entering interactive state. A factory panic or premature worker exit is also a startup failure (recv error), never a hang. Tests inject a fake factory (`Ok`/`Err`).
  - **Submission failure**: worker death drops the job receiver, so `SyncSender::send` returns `SendError` → the controller treats it as a recoverable transcription failure, prints an error, and returns Ready (already implemented in slice 2/3 `finish_recording`; kept).
  - **Panic policy**: per-job `catch_unwind(AssertUnwindSafe(..))` around `transcribe` + text extraction; a panic becomes `TranscriptionFailed` (message includes the panic payload) and the worker stays alive for the next job. Only the outer `recv`/`send` loop is outside the catch; those do not panic, so a panicking job can never strand the controller in `Transcribing`. A worker-thread panic outside a job (not possible today) would drop the job receiver and surface as a submission failure on the next cycle.
  - **Shutdown signal/acknowledgement**: the worker exits its loop when `jobs.recv()` errors (all senders dropped — `main` drops `App`, which owns the only job sender) or when an outcome `events.send` fails (event receiver dropped when `app::run` returns). No dedicated shutdown message is needed; channel closure is the signal. `App::shutdown()` sets the active transcription's cancellation flag so an in-flight inference aborts at the next encode/decode boundary instead of running to completion.
  - **Join timeout**: `WorkerHandle::join_with_timeout(WORKER_JOIN_TIMEOUT = 2 s)` polls `JoinHandle::is_finished` and then joins; on timeout `main` proceeds to exit and process teardown terminates the worker thread (the worker owns the context/state, so no double-free: the thread is killed by the OS).
  - **Repeated Ctrl+C**: the first Ctrl+C (raw-mode key event) returns from `app::run` and starts shutdown; the terminal is restored before the join, so a second Ctrl+C during the ≤2 s shutdown window is a normal SIGINT that kills the process (escape hatch); the join bounds the wait and shutdown never hangs. | Constants/`spawn_worker`/`worker_loop`/`process_job`/`WhisperTranscriber`/`WorkerHandle` in `src/transcriber.rs`; `App::shutdown` in `src/app.rs`; startup wiring in `src/main.rs`; worker protocol tests (one job at a time, outcome classification, panic survival, channel-close exit, join timeout, handshake) in `transcriber::tests`; controller tests (submit-once, event gating) in `app::tests`. `set_abort_callback_safe` signature verified in whisper-rs 0.16.0 `whisper_params.rs`; abort returns `-6`/`-8`/`-9` verified in vendored whisper.cpp 1.8.3 (`whisper.cpp` lines 7020/7146/7458 + `return !(abort_callback && ...)` at 2455/2977); default `print_progress`/`print_timestamps`/`language="en"` verified at `whisper_full_default_params` (line 5906); `detect_language` override verified at line 6812. |

## Slice 5 Gate Resolutions (D3 — arboard policy)

Resolved 2026-08-10 while implementing slice 5. The arboard half of blocking row D3 (deferred from slice 1) is now satisfied; packaging remains deferred to slice 8.

| ID | Resolution | Evidence |
| --- | --- | --- |
| D3 (arboard) | **Version**: `arboard = 3.6.1` (latest stable as of 2026-08-10, MSRV 1.71, edition 2021), pinned with `default-features = false` (drops the `image-data`/PNG image machinery — this app only copies text) plus `features = ["wayland-data-control"]` (pulls `wl-clipboard-rs 0.9.0`). **Session prerequisites**: X11 works through `x11rb 0.13` (pure-Rust xcb protocol, no system libxcb); Wayland works through `wl-clipboard-rs`, which daemonizes a child process to serve content (no persistent client connection). `Clipboard::new()` prefers Wayland when `WAYLAND_DISPLAY` is set and falls back to X11; with neither service reachable it fails. **Failure policy**: a missing/headless clipboard service must never prevent startup or invalidate a successful transcription (functional spec 15.4) — `ArboardClipboard` constructs one `arboard::Clipboard` lazily at the first copy and keeps it alive for the session (all on the main thread). Keeping a single persistent instance is required on X11: arboard hosts the selection inside the app, and when the last `Clipboard` instance drops it hands the content to a clipboard manager and tears the serving window down (`impl Drop for Clipboard` in `x11.rs`), so constructing per copy would race clipboard managers and can lose the content (confirmed by arboard's own `Clipboard was dropped very quickly after writing` warning during manual testing). A construction/set failure surfaces as a per-copy `ClipboardError` and the controller prints a non-fatal warning; the app remains usable for later cycles. `ClipboardError(pub String)` follows the established `RecorderError` pattern; the app tests inject a fake `Clipboard` and never touch the real one. | arboard 3.6.1 source downloaded from `static.crates.io` on 2026-08-10 and inspected: `src/lib.rs` (`Clipboard::new()`/`set_text(&mut self, Into<Cow<str>>)` signatures), `src/platform/linux/mod.rs` (Wayland-first `new()` fallback), `src/platform/linux/x11.rs` (process-global `static CLIPBOARD` at line 58, `new()` reuses the cached inner, `impl Drop for Clipboard` at line 1082 with the manager handoff and fast-drop warning), `src/platform/linux/wayland.rs` (daemonized `copy::copy`), `src/common.rs` (`Error` variants, Display is a short description without payload). wl-clipboard-rs 0.9.0 `Cargo.toml` deps inspected (wayland-client 0.31.3, wayland-protocols 0.32.1, wayland-protocols-wlr 0.3.1, os_pipe, rustix, tempfile). Manual pty/display checks on 2026-08-10: full X11 cycle on `DISPLAY=:1` with speech played through an snd-aloop capture (isolated `XDG_DATA_HOME`, seeded verified model): `Transcription:` + `Copied to clipboard.` + `Ready to record`, clipboard content read back during the run equals the printed text (via an arboard-based reader), no fast-drop warning with the persistent instance; headless run (no `DISPLAY`/`WAYLAND_DISPLAY`) prints the transcription plus a clipboard warning and stays usable. Wayland could not be exercised (no compositor on the test machine); its path is code-verified (daemonized copy). |

### Slice 5 notes

- **Transcription defect found during manual verification**: `WhisperTranscriber` previously called `params.set_detect_language(true)` (a decision-D2-era choice). Empirically, `detect_language` returns **zero segments** on the pinned multilingual `ggml-base.bin` with whisper-rs 0.16.0 / vendored whisper.cpp 1.8.3 — verified 2026-08-10 against the canonical `jfk.wav`: greedy and beam both give `segments=0` with detection on (language detected `en` p≈0.95) while the whisper.cpp default language (`"en"`, verified at `whisper_full_default_params`) yields the expected transcript (2 segments). Every real transcription was therefore empty, which blocks the slice-5 manual checks (`copied text equals terminal result`). Fix: removed `set_detect_language(true)` and rely on the whisper.cpp default `"en"` — a surgical change to the comment + one removed line in `src/transcriber.rs`, no prior verified acceptance behavior replaced (slice 4 never exercised real audio; its worker-protocol tests use a fake transcriber). The D2 note claiming detection was "verified" only covered the code path, not output.

### Slice 4 notes

- Manual pty runs on 2026-08-10 (isolated `XDG_DATA_HOME`, `TERM=xterm script`): cold-cache first run downloaded `ggml-base.bin`, re-verified its SHA-256, and installed it before Ready; the worker startup handshake completed before the Ready render (`whisper_init_state` logs precede it); a full cycle rendered `Recording...` → `Transcribing...` → labeled `Transcription:` output → `Ready`; `Ctrl+C` exited 0 with cursor restored. Repeated runs reused the cached model (no download logs) and a network monitor showed no model download traffic during the session (5.6 KB ambient only) — inference is local.
- The plan's spoken-recording checks (real speech → non-empty text, two+ cycles reusing the model worker) could not be fully exercised: every audio-input path on this test machine is broken (snd-aloop's mirror stops passing audio once PipeWire holds the device; the ALC897 HDA loopback mix carries no program audio; no speakers are coupled to any microphone; cpal's format heuristics pick F32 which snd-aloop cannot mirror). This is an environment limitation, not an app defect: the audio→worker→text path is covered by the worker-protocol tests with a fake transcriber, and the whisper-rs integration (`WhisperTranscriber`) was verified against the crate source (Phase-1 gate evidence above).

## Non-Goals

Do not add settings, global shortcuts, GUI, cloud services, stored audio/history, partial results, concurrent recordings, configurable keys, VAD, or a general job framework. These are outside the initial release.
